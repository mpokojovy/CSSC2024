# (C) Michael Pokojovy (2024)

# install.packages("robustbase")

PATH = NULL # set path

setwd(PATH)

rm(list = ls())
source("main.loc.R")

rm(list = ls())
source("main.scale.R")

rm(list = ls())
source("main.bias.R")